import React from "react";
import Navbar from "../sharedComponents/Navbar";

const Home = (props) => {
  return (
    <div className="container">
      <Navbar />
    </div>
  );
};

export default Home;
